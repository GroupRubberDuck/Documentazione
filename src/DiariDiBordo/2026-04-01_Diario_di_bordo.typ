#import "../TypstTemplate/diarioTemplate.typ": slides
#import "/src/config.typ": *

#show: slides.with(
  numero: 12,
  data: "2026-04-01",
)

= Attività completate
#set list(marker: image(check_icon, width: 0.8em))
- Ricevuto semaforo verde dal Professor Cardin
- Ulteriore studio delle tecnologie per confermare quelle adottate e aggiungere Vue.js (framework frontend), in seguito al feedback del Professor Cardin
- Corretti gli errori minori dell'Analisi dei Requisiti segnalati
- Dopo un'analisi della situazione attuale è stata spostata la data di consegna dal 30 aprile al 15 maggio

= Attività da completare
#set list(marker: image(clessidra_icon, width: 0.7em))
- Terminare l'approvazione dei documenti e la presentazione per l'incontro di revisione RTB

= Dubbi
#set list(marker: image(question_mark_icon, width: 1em))
- Contenuti cardine della presentazione di revisione RTB

