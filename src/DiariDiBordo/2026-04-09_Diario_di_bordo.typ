#import "../TypstTemplate/diarioTemplate.typ": slides
#import "/src/config.typ": *

#show: slides.with(
  numero: 14,
  data: "2026-04-09",
)

= Attività completate
#set list(marker: image(check_icon, width: 0.8em))
- Superato la revisione RTB con il professor Vardanega.
- Formalizzato il passaggio alla fase di Product Baseline (PB).
- Passaggio a sprint settimanali per miglior monitoraggio delle task.

= Attività da completare
#set list(marker: image(clessidra_icon, width: 0.7em))
- Studio dell'architettura logica e deplyment per il documento di Specifica Tecnica.
- Stesura dell'introduzione del documento di Specifica Tecnica.
- Creazione nuova repository per MVP e valutare adozione di pull request.

= Dubbi
#set list(marker: image(question_mark_icon, width: 1em))
- Modalità della strutturazione e contenuti cardine del documento di Specifica Tecnica.
- Ridistribuzione ore da Analista verso altri ruoli per garantire coerenza con il piano economico.

