#import "../TypstTemplate/diarioTemplate.typ": slides
#import "/src/config.typ": *

#show: slides.with(
  numero: 16,
  data: "2026-04-23",
)


= Attività completate
#set list(marker: image(check_icon, width: 0.8em))
- Redatte le sezioni di architettura logica, deployment, diagrammi di sequenza e attività, schema dati e design pattern nel documento di Specifica Tecnica.

- Completati i diagrammi delle classi principali nel documento di Specifica Tecnica.
- Setup argomenti principali del manuale utente.

= Attività da completare
#set list(marker: image(clessidra_icon, width: 0.7em))
- Proseguire con la stesura della sezione frontend e continuare la redazione del documento di Specifica Tecnica.

- Setup dell'ambiente di lavoro per lo sviluppo dell'MVP.
- Organizzare un incontro con il professor Cardin per chiarire i dubbi residui sulla Specifica Tecnica.