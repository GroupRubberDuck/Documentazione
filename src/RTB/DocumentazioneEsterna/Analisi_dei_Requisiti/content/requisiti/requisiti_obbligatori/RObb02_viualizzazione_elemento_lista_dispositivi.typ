#import "config/deps.typ" as deps
#let data =(
  codice:[
    #deps.get-req-code(nome-etichetta: "Viualizzazione elemento lista dispositivi")
    #label(deps.slugify("Viualizzazione elemento lista dispositivi"))
    ],

  descrizione:[

  ],
  fonti:[
    
  ]
)